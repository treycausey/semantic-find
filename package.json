{
  "name": "semantic-find",
  "version": "0.1.0",
  "private": true,
  "description": "Bounded local evidence search with optional Jev reranking",
  "type": "module",
  "bin": {
    "semantic-find": "./src/cli.ts"
  },
  "scripts": {
    "typecheck": "bun x tsc --noEmit",
    "test": "bun test",
    "check": "bun run typecheck && bun test",
    "start": "bun run src/cli.ts"
  },
  "dependencies": {
    "@typesafe-ai/sdk": "0.6.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "latest"
  }
}
